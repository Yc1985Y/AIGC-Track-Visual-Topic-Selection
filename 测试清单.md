# 测试清单

本清单用于后续对 `VisualSemanticAgent` 进行实际真机验证。建议按“构建准备 -> 基础环境 -> 主场景 -> 异常容错 -> 体验细节”的顺序执行。每项都尽量记录结果，方便后续回查问题。

## 一、测试前准备

### 1. 工程准备

- 在 Android Studio 中打开 `VisualSemanticAgent/`
- 先执行一次 `Gradle Sync`
- 再执行一次 `Build > Make Project` 或 `Build APK`
- 如使用真实接口，在 `VisualSemanticAgent/app/build.gradle` 中填入真实 `VLM_API_KEY`
- 如先做本地演示，可直接使用默认 `debug` Mock 模式
- 确认 `VLM_MODEL_NAME` 仍是当前计划测试的模型

### 2. 设备准备

- 使用一台真机，推荐 Android 10 及以上
- 设备已联网
- 设备至少具备：
  - 相机
  - 中文 TTS
  - 系统语音识别
- 最好同时安装：
  - 日历应用
  - 地图应用
  - 短信应用

### 3. 测试素材准备

- 准备至少 3 张活动海报：
  - 一张带“中文日期 + 中文地点”
  - 一张带“纯数字日期时间”
  - 一张带“年份 + 月日 + 时分”
- 准备至少 2 张地点图片：
  - 一张只有地点名
  - 一张地点名里包含空格或较长中文名称
- 准备至少 2 张普通室内图片：
  - 教室/实验室/宿舍
  - 桌面/书架/走廊
- 准备至少 2 张困难样本：
  - 模糊图片
  - 光线较暗图片

## 二、实际测试记录模板

每做完一项，建议记录：

- 测试编号
- 测试时间
- 设备型号
- Android 版本
- 当前网络环境
- 当前模型名
- 测试输入：
  - 使用的图片名称
  - 使用的文字指令
  - 是否使用语音
- 实际结果
- 是否符合预期
- 是否稳定复现
- 截图或录屏文件名
- 备注

## 三、基础可用性测试

### 1. 应用启动

如何实际测试：

- 安装最新 Debug 包
- 首次启动应用
- 观察首页是否正常显示
- 等待 3 到 5 秒，确认相机预览是否出来

预期：

- 应用可正常进入首页
- 无闪退
- 相机预览区域可见
- 页面底部预设按钮、输入框、拍照按钮可见
- 若当前是 `debug` 构建，应看到 Mock 模式提示文案

### 1.1 Mock 模式动作验证

如何实际测试：

- 保持 `debug` 构建默认配置
- 依次输入以下文字，并分别点击拍照执行：
  - `请把这个活动加入日历`
  - `帮我导航去这个地方`
  - `请帮我发短信通知老师`
  - `描述一下这张图`
- 最后输入 `mock_error` 再点击拍照执行

预期：

- 第一条会拉起日历
- 第二条会拉起地图
- 第三条会拉起短信应用
- 第四条会返回描述类结果并触发 TTS
- `mock_error` 会弹出错误弹层，便于检查关闭和重试

### 2. 相机权限

如何实际测试：

- 卸载重装应用，或在系统设置中清空权限
- 首次启动时拒绝相机权限
- 点击拍照按钮
- 再点击错误弹层中的“去授权”
- 在系统权限页重新允许相机权限
- 返回应用再次观察

预期：

- 拒绝权限后有明确错误提示
- 错误弹层提供“去授权”
- 未授权时不能执行拍照识别
- 重新授权后相机预览恢复正常

### 3. 麦克风权限

如何实际测试：

- 保留相机权限
- 首次启动时如果没有主动弹出麦克风权限请求，属于当前预期行为
- 在系统权限里关闭麦克风权限
- 返回应用
- 点击麦克风按钮
- 再尝试文字输入和拍照按钮

预期：

- 应用仍可拍照和文字输入
- 首页可提示“如需语音输入，可稍后再授权麦克风权限”
- 点击语音按钮时提示需要麦克风权限
- 错误弹层提供“去授权”
- 不会因为没有麦克风权限导致首页不可用

### 3.1 错误弹层交互

如何实际测试：

- 在未授予相机权限时直接点击拍照按钮
- 等待错误弹层出现
- 先点击“关闭”
- 再次触发错误弹层后点击“去授权”或“重试”
- 观察两个按钮是否都能被点击，并且不会误触到底部拍照/语音按钮

预期：

- 错误弹层按钮可正常点击
- 点击“关闭”后弹层消失
- 点击“去授权”或“重试”后触发对应动作
- 弹层显示期间，底层页面不会被误触

## 四、主场景测试

### 4. 活动海报 -> 日历

如何实际测试：

- 选择一张活动海报
- 点击“活动入日历”预设
- 把海报尽量拍清楚，保证标题、时间、地点都在画面里
- 点击拍照执行
- 若成功，关闭日历页后回到应用，再换第二张海报继续测

预期：

- 出现加载态
- 返回结果卡片
- 状态文案提示“已识别活动”或“正在打开日历”
- 成功拉起系统日历创建页
- 日历标题、地点、时间尽量被自动填充

重点记录：

- 哪种时间格式最容易成功：
  - `yyyy-MM-dd HH:mm`
  - `yyyy/MM/dd HH:mm`
  - `yyyy.MM.dd HH:mm`
  - `yyyy年M月d日 HH:mm`
  - `M月d日 HH:mm`
  - `yyyy/MM/dd`
  - `yyyy.MM.dd`
  - `yyyy年M月d日`
- 当时间未识别成功时，标题和地点是否仍可正常填充
- 如果只识别到日期没有识别到时分，默认 09:00 是否符合预期

### 5. 地点识别 -> 地图导航

如何实际测试：

- 选择一张带明确地点名的图片
- 点击“地点去导航”预设
- 点击拍照执行
- 观察是否拉起地图
- 再用一张“地点名较长/带空格”的图片重复一次
- 最后再尝试输入文字：“帮我导航去这个地方”

预期：

- 返回结果卡片
- 状态文案提示“已识别地点”
- 成功拉起地图
- 地图中出现目标地点或导航搜索结果

重点记录：

- 中文地点名是否都能跳转成功
- 带空格或较长地点名是否还能正常搜索
- 没有 Google Maps 时，通用地图 Intent 是否正常兜底

### 6. 场景描述

如何实际测试：

- 点击“场景描述”预设
- 对教室、桌面、走廊等普通场景拍照
- 执行 2 到 3 次

预期：

- 不强行打开外部应用
- 结果卡片显示描述内容
- TTS 会播报结果
- 状态文案与结果卡片内容大体一致

### 7. 寻物导视

如何实际测试：

- 点击“寻物导视”预设
- 对桌面、书架、教室等多目标场景拍照
- 尝试拍摄“物品较多”和“视角较乱”的场景

预期：

- 返回导视/描述类文本
- 不崩溃
- TTS 能播报
- 若模型无法确定动作，至少返回可读提示

## 五、输入方式测试

### 8. 手动文字输入

如何实际测试：

- 不使用预设，直接输入以下指令逐条测试：
  - `帮我把这个活动加入日历`
  - `帮我导航去这个地方`
  - `描述一下这张图`
- 每输入一条都重新拍照执行一次

预期：

- 输入框可正常编辑
- 加载中和语音识别中输入框会被禁用
- 拍照结果与文字意图大体一致

### 9. 短信动作容错

如何实际测试：

- 找一张带手机号的宣传单、名片或直接输入文字提示模型“给这个号码发提醒短信”
- 准备三种号码形式分别尝试：
  - `138 0013 8000`
  - `138-0013-8000`
  - `+86 138-0013-8000`
- 观察是否能成功拉起短信应用

预期：

- 模型返回 `send_sms` 时可正常拉起短信应用
- 号码中的空格、短横线不会导致跳转失败
- 带 `+86` 的号码不会因 URI 拼接问题导致短信页面打不开

### 10. TTS 兜底观察

如何实际测试：

- 对普通场景执行“场景描述”或“寻物导视”
- 连续执行 2 到 3 次，观察应用是否稳定播报
- 如果测试机没有可用中文 TTS，也要继续观察主流程是否仍然能返回结果卡片和状态文本

预期：

- 有 TTS 时能正常播报
- 没有可用 TTS 时不应影响拍照识别主流程
- 不会因为 TTS 初始化问题导致页面卡死或反复弹错

- 不同指令能影响模型动作类型
- 结果卡片与状态文案匹配
- 即使图片相同，不同文字指令也能产生不同动作倾向

### 9. 语音输入

如何实际测试：

- 点击麦克风按钮
- 清晰说出一句中文指令，例如：
  - `帮我把这个活动加入日历`
  - `帮我导航去这个地方`
- 观察识别结果是否填入输入框
- 再分别测试：
  - 声音较小
  - 环境有噪音
  - 说话说到一半停下

预期：

- 状态变为“正在听你说话…”
- 成功识别后文本框自动填入语音结果
- 随后可继续点击拍照执行
- 失败时有明确提示
- 可点击重试

重点记录：

- 连续快速点击麦克风按钮时是否仍然稳定
- 失败后是否会残留“监听中”状态
- 语音失败时更接近哪一类提示：
  - 没有识别清楚
  - 识别器忙
  - 语音服务异常
- 语音出错时，是否能重新点击后恢复

## 六、异常与容错测试

### 10. 未配置 API Key

如何实际测试：

- 将 `VLM_API_KEY` 临时置空
- 重新安装运行
- 点击拍照执行

预期：

- 提示未配置 API Key
- 不会真的发请求
- 不应出现崩溃

### 11. 断网测试

如何实际测试：

- 打开应用并保持相机可用
- 关闭 Wi-Fi 和移动数据
- 点击任意预设并拍照执行
- 观察错误弹层
- 点击“重试”一次

预期：

- 提示网络连接失败
- 有重试入口
- 不崩溃
- 重试后仍应给出可理解的失败提示，而不是卡住

### 12. 网络抖动测试

如何实际测试：

- 在弱网环境下，或通过热点切换制造网络不稳定
- 连续执行 3 到 5 次拍照识别
- 记录每次成功/失败情况

预期：

- 短时失败时可以恢复
- 不出现界面卡死
- 不出现一次失败后后续都不可用的情况

### 13. 模型返回不稳定

如何实际测试：

- 使用复杂图片、模糊图片、光线不足图片
- 每类至少测试 1 次
- 观察是否出现解析失败、动作不明确或错误动作

预期：

- 即使模型没有给出稳定动作，也应返回可读提示
- 不因 JSON 解析失败直接崩溃
- 若无法处理，应出现可关闭或可重试的错误弹层

### 14. 无日历 / 无地图 / 无短信应用

如何实际测试：

- 在无对应 App 的设备上测，或暂时停用对应应用
- 分别触发：
  - 活动入日历
  - 地点去导航
  - 如后续支持短信演示，可加短信测试

预期：

- 显示“当前设备没有可用应用”的错误提示
- 不崩溃
- 返回应用后可继续其他测试

## 七、交互与体验测试

### 15. 连续执行

如何实际测试：

- 连续执行 5 次拍照识别
- 建议在 5 次里混合使用：
  - 活动海报
  - 地点图片
  - 普通场景图

预期：

- 相机预览不会丢失
- 不会越来越卡
- 结果卡片会更新
- 不会频繁出现“相机未准备好”

### 16. 回前台与中断恢复

如何实际测试：

- 打开应用后按 Home 键退到后台
- 5 秒后回到前台
- 再锁屏解锁回到应用
- 如果设备支持旋转，可顺带测一次横竖屏切换

预期：

- 相机可恢复
- 无明显异常弹层残留
- 不会因为切后台就出现误报错误

### 17. 加载与错误弹层

如何实际测试：

- 人为触发一次错误：
  - 断网
  - 拒绝权限
  - 拍模糊图
- 先点击“关闭”
- 再次执行操作
- 再触发一次错误，点击“重试”或“去授权”

预期：

- 错误弹层可消失
- 不会阻塞后续操作
- 点击“重试”或“去授权”后，操作能按预期继续

## 八、建议追加的重点观察项

测试时建议特别留意：

- 返回日历页后再回应用，相机是否仍正常
- 返回地图页后再回应用，相机是否仍正常
- TTS 是否会重复播报上一次结果
- 当模型只返回描述、不返回动作时，界面是否仍可读
- 连续失败两次后，错误文案是否仍然清楚

## 九、当前最值得优先验证的 5 项

如果时间有限，优先做这五项：

1. 启动应用并确认相机预览正常
2. 海报识别 -> 打开日历
3. 地点识别 -> 打开地图导航
4. 断网后错误提示与重试
5. 语音输入成功与失败各测一次
