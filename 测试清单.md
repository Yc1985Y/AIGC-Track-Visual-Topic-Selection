# 测试清单

本清单用于后续对 `VisualSemanticAgent` 进行真机验证。建议按“基础环境 -> 主场景 -> 异常容错 -> 体验细节”的顺序执行，并记录每项结果。

## 一、测试前准备

- 设备已安装可调试 APK
- 设备已联网
- `VLM_API_KEY` 已在 `VisualSemanticAgent/app/build.gradle` 中配置
- 设备至少具备：
  - 相机
  - 中文 TTS
  - 系统语音识别
- 最好同时安装：
  - 日历应用
  - 地图应用
  - 短信应用

## 二、基础可用性测试

### 1. 应用启动

- 启动应用
- 预期：
  - 应用可正常进入首页
  - 无闪退
  - 相机预览区域可见

### 2. 相机权限

- 首次启动拒绝相机权限
- 预期：
  - 出现明确错误提示
  - 错误弹层提供“去授权”
  - 未授权时不能执行拍照识别

- 重新授权相机权限
- 预期：
  - 应用恢复可拍照状态
  - 相机预览正常显示

### 3. 麦克风权限

- 保留相机权限，拒绝麦克风权限
- 预期：
  - 应用仍可拍照和文字输入
  - 点击语音按钮时提示需要麦克风权限
  - 错误弹层提供“去授权”

## 三、主场景测试

### 4. 活动海报 -> 日历

- 准备一张包含活动名称、时间、地点的海报
- 点击“活动入日历”预设
- 拍照执行
- 预期：
  - 出现加载态
  - 返回结果卡片
  - 状态文案提示“已识别活动”或“正在打开日历”
  - 成功拉起系统日历创建页
  - 日历标题、地点、时间尽量被自动填充

- 重点记录：
  - 时间是否被正确识别
  - 中文日期/纯数字时间/带年份日期哪类效果最好

### 5. 地点识别 -> 地图导航

- 准备一张带明显地点名的图片，或直接在输入框加指令
- 点击“地点去导航”预设
- 拍照执行
- 预期：
  - 返回结果卡片
  - 状态文案提示“已识别地点”
  - 成功拉起地图
  - 地图中出现目标地点或导航搜索结果

### 6. 场景描述

- 点击“场景描述”预设
- 对普通室内场景拍照
- 预期：
  - 不强行打开外部应用
  - 结果卡片显示描述内容
  - TTS 会播报结果

### 7. 寻物导视

- 点击“寻物导视”预设
- 对桌面、书架、教室等多目标场景拍照
- 预期：
  - 返回导视/描述类文本
  - 不崩溃
  - TTS 能播报

## 四、输入方式测试

### 8. 手动文字输入

- 不使用预设，直接输入：
  - “帮我把这个活动加入日历”
  - “帮我导航去这个地方”
  - “描述一下这张图”
- 预期：
  - 不同指令能影响模型动作类型
  - 结果卡片与状态文案匹配

### 9. 语音输入

- 点击麦克风按钮，说出清晰中文指令
- 预期：
  - 状态变为“正在听你说话…”
  - 成功识别后文本框自动填入语音结果
  - 随后可继续点击拍照执行

- 低音量、噪声环境、说话中断再试
- 预期：
  - 失败时有明确提示
  - 可点击重试

## 五、异常与容错测试

### 10. 未配置 API Key

- 将 `VLM_API_KEY` 置空后安装运行
- 预期：
  - 点击拍照执行后提示未配置 API Key
  - 不应出现崩溃

### 11. 断网测试

- 关闭网络后执行一次拍照识别
- 预期：
  - 提示网络连接失败
  - 有重试入口
  - 不崩溃

### 12. 网络抖动测试

- 在弱网环境重复测试 3 到 5 次
- 预期：
  - 短时失败时可以恢复
  - 不出现界面卡死

### 13. 模型返回不稳定

- 使用复杂图片、模糊图片、光线不足图片
- 预期：
  - 即使模型没有给出稳定动作，也应返回可读提示
  - 不因 JSON 解析失败直接崩溃

### 14. 无日历 / 无地图 / 无短信应用

- 在缺少对应系统应用的设备或模拟环境测试
- 预期：
  - 能显示“当前设备没有可用应用”的错误提示
  - 不崩溃

## 六、交互与体验测试

### 15. 连续执行

- 连续执行 5 次拍照识别
- 预期：
  - 相机预览不会丢失
  - 不会越来越卡
  - 结果卡片会更新

### 16. 横竖屏与回前台

- 进入后台再回到前台
- 锁屏后解锁再回应用
- 预期：
  - 相机可恢复
  - 无明显异常弹层残留

### 17. 加载与错误弹层

- 人为触发错误后点击“关闭”
- 再次执行操作
- 预期：
  - 错误弹层可消失
  - 不会阻塞后续操作

- 人为触发错误后点击“重试”或“去授权”
- 预期：
  - 操作能按预期继续

## 七、建议记录项

每次测试建议记录：

- 测试时间
- 设备型号
- Android 版本
- 网络环境
- 测试图片/指令
- 实际结果
- 是否符合预期
- 问题描述
- 截图或录屏文件名

## 八、当前最值得优先验证的 5 项

如果时间有限，优先做这五项：

1. 启动应用并确认相机预览正常
2. 海报识别 -> 打开日历
3. 地点识别 -> 打开地图导航
4. 断网后错误提示与重试
5. 语音输入成功与失败各测一次
