# 详细检查清单

本清单用于对当前项目做“从代码到真机”的逐步检查。和 `测试清单.md` 不同，这份更强调检查顺序、检查目标、通过标准，以及出现问题时该先看哪里。

建议执行方式：

1. 先完成“代码与配置静态检查”
2. 再完成“本地工程检查”
3. 最后进入“真机功能检查”
4. 每完成一步，记录结果，不要跳步

---

## 第一阶段：代码与配置静态检查

### Step 1. 检查项目入口与主状态管理

检查文件：

- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/MainActivity.kt`

检查点：

- 是否存在拍照、语音、错误弹层、加载态的统一状态管理
- 是否区分了相机权限与麦克风权限
- 是否对 API Key 为空做了拦截
- 是否有统一错误映射逻辑
- 是否在 `onDestroy()` 中释放相机、语音识别、TTS

通过标准：

- 所有主流程入口都集中在 `MainActivity`
- 权限不是“一刀切”
- 错误不会直接把原始异常字符串暴露给用户

### Step 2. 检查相机绑定与拍照链路

检查文件：

- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/camera/CameraManager.kt`
- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/utils/EncodingUtils.kt`

检查点：

- 相机是否避免了重复绑定
- 拍照后是否正确关闭 `ImageProxy`
- `ImageProxy -> Bitmap` 是否考虑 `rowStride/pixelStride`
- 图片是否进行了旋转修正
- 是否有 executor 的创建与释放

通过标准：

- 不会因反复重组 UI 导致重复绑定
- 不会因 `ImageProxy` 未关闭导致卡死
- 图像编码逻辑对真机兼容更稳
- 拍照协程被取消后，不会因为晚到回调再次抛异常

### Step 3. 检查模型请求与响应解析

检查文件：

- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/network/VLMNetworkClient.kt`
- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/model/VLMModels.kt`
- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/utils/ResponseInterpreter.kt`

检查点：

- 请求地址、模型名、鉴权头是否正确
- 是否自动带上 `request_id`
- 是否有网络重试
- 是否区分网络失败、接口失败、解析失败
- `action` 字段缺失时是否会崩
- 结果字段是否有清洗与归一化

通过标准：

- 请求结构符合 vivo 比赛接口
- 模型返回夹杂解释文本时仍可清洗 JSON
- `action` 缺失时会自动回退到 `unknown`
- JSON 后面即使夹带尾随文本，也不会直接把整段脏文本当成 JSON 解析

### Step 4. 检查系统动作分发

检查文件：

- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/intent/IntentDispatcher.kt`

检查点：

- `create_event` 是否尝试写入标题、地点、时间
- `navigate` 是否兼容 Google Maps 和通用地图 Intent
- `send_sms` 是否校验手机号与内容
- 没有外部应用时是否返回清晰错误
- 时间字符串是否支持多种格式

通过标准：

- 日历、地图、短信三条动作都能明确失败原因
- 时间字段不只支持纯毫秒时间戳
- 短信号码中的 `+`、空格、短横线不会直接破坏 `smsto:` 跳转

### Step 5. 检查语音与 TTS 能力

检查文件：

- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/voice/VoiceRecognitionManager.kt`
- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/tts/TextToSpeechManager.kt`

检查点：

- 语音识别是否判断系统可用性
- 语音识别期间是否避免重复点击
- 取消时是否调用 `recognizer.cancel()`
- TTS 是否有初始化失败兜底
- 中文 TTS 不可用时是否至少有回退日志

通过标准：

- 不会因语音按钮连点导致识别器忙状态异常
- TTS 初始化失败不会拖垮主流程
- TTS 初始化失败后不会无限堆积待播报文本

### Step 6. 检查 UI 交互层

检查文件：

- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/ui/CameraScreen.kt`
- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/ui/LoadingOverlay.kt`
- `VisualSemanticAgent/app/src/main/res/values/strings.xml`

检查点：

- 加载态是否阻断误触
- 错误弹层是否支持关闭和重试
- 结果卡片是否能展示结构化结果
- 预设按钮、语音按钮、拍照按钮在忙状态下是否禁用
- 用户可见文案是否齐全

通过标准：

- 忙状态下不会继续触发重复操作
- 用户能看懂当前是“识别中、失败、缺权限、缺应用”哪一种
- 错误弹层出现时，“关闭/重试”按钮本身仍然可点击

### Step 7. 检查 Manifest 与权限声明

检查文件：

- `VisualSemanticAgent/app/src/main/AndroidManifest.xml`

检查点：

- 是否声明 CAMERA、RECORD_AUDIO、INTERNET、ACCESS_NETWORK_STATE
- 是否声明地图/日历/短信查询
- `MainActivity` 是否可启动

通过标准：

- 真机需要的权限和 queries 都在

### Step 8. 检查测试文件与文档一致性

检查文件：

- `VisualSemanticAgent/app/src/test/java/com/vsa/visualsemanticagent/ModuleTests.kt`
- `README.md`
- `VisualSemanticAgent/README.md`
- `进度说明.md`
- `测试清单.md`

检查点：

- 单测是否覆盖响应清洗、动作归一化、状态文案
- README 是否和当前接入方式一致
- `进度说明.md` 是否记录最新修复
- 测试清单是否覆盖主场景与异常场景

通过标准：

- 文档不明显落后于代码
- 最少有一批单测覆盖核心纯逻辑

---

## 第二阶段：本地工程检查

### Step 9. 检查 BuildConfig 配置

检查文件：

- `VisualSemanticAgent/app/build.gradle`

检查点：

- `VLM_API_KEY`
- `VLM_MODEL_NAME`
- `VLM_API_ENDPOINT`

通过标准：

- 三个字段已配置或明确留空待填

### Step 10. Android Studio 同步检查

操作：

- 用 Android Studio 打开 `VisualSemanticAgent`
- 执行 Gradle Sync

检查点：

- 是否有依赖下载失败
- 是否有 Kotlin/Compose 版本冲突
- 是否有资源缺失

通过标准：

- Sync 成功

### Step 11. Debug 构建检查

操作：

- 在 Android Studio 中执行 `Build APK` 或运行 `debug`

检查点：

- 是否存在 Kotlin 编译错误
- 是否存在 Compose 参数签名不匹配
- 是否存在资源引用错误

通过标准：

- Debug 包可成功构建

---

## 第三阶段：真机功能检查

### Step 12. 首次启动检查

操作：

- 安装 APK
- 首次启动应用

检查点：

- 是否正常进入首页
- 相机预览是否显示
- 首次权限请求是否弹出

通过标准：

- 无闪退
- 有首页

### Step 13. 权限分流检查

操作：

- 只给相机，不给麦克风

检查点：

- 是否仍可拍照
- 点击语音时是否提示去授权

通过标准：

- 相机链路不受麦克风权限影响

### Step 14. 拍照主链路检查

操作：

- 输入或选择预设
- 拍照执行

检查点：

- 是否进入加载态
- 是否产生结果卡片
- 是否有 TTS 或外部动作

通过标准：

- 主链路能完整走完

### Step 15. 活动入日历检查

操作：

- 用活动海报测试

检查点：

- 是否识别活动标题
- 是否尽量解析时间
- 是否能拉起日历

通过标准：

- 日历创建页能被打开

### Step 16. 地点导航检查

操作：

- 用地点图片或地点文字测试

检查点：

- 是否识别地点
- 是否能拉起地图

通过标准：

- 地图页能被打开

### Step 17. 语音输入检查

操作：

- 点击语音输入并说一句清晰中文

检查点：

- 文本框是否被填充
- 识别期间按钮是否被锁定
- 失败时是否有清晰提示

通过标准：

- 成功和失败两种路径都可预期

### Step 18. 异常链路检查

操作：

- 断网
- 模糊拍摄
- 无日历/无地图设备

检查点：

- 是否给出明确错误分类
- 是否支持关闭或重试
- 是否不会直接崩溃

通过标准：

- 异常可控、可复现、可解释

---

## 第四阶段：问题记录模板

每发现一个问题，按以下格式记录：

- 检查步骤：
- 设备型号：
- Android 版本：
- 输入内容：
- 预期结果：
- 实际结果：
- 是否稳定复现：
- 关联文件猜测：
- 截图/录屏：

---

## 当前建议的执行顺序

如果你想一步一步最省时间地查，按这个顺序最合适：

1. `MainActivity.kt`
2. `CameraManager.kt`
3. `EncodingUtils.kt`
4. `VLMNetworkClient.kt`
5. `IntentDispatcher.kt`
6. `VoiceRecognitionManager.kt`
7. `TextToSpeechManager.kt`
8. `CameraScreen.kt` / `LoadingOverlay.kt`
9. `strings.xml`
10. `AndroidManifest.xml`
11. `ModuleTests.kt`
12. Android Studio Sync
13. Debug 构建
14. 真机主场景
15. 真机异常场景
