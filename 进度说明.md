# 进度说明

## 约定

- 之后每次继续这个项目时，优先读取本文件，再开始分析、修改和提交。
- 本文件用于记录当前阶段、已完成内容、待办事项和关键阻塞。

## 当前项目结构

- `方案.md`
  - 方案与思路说明。
- `VisualSemanticAgent/`
  - Android 原型工程主体。
- `PX29Bq7U_1773800487463.pdf`
  - 参考材料。
- `视觉语义执行代理方案设计.docx`
  - 方案文档备份版本。

## 当前阶段判断

- 当前阶段：`MVP 原型补全中`
- 当前目标：先把 `VisualSemanticAgent` 收敛成可运行、可演示的最小闭环。
- 当前优先场景：
  - 拍照/输入指令 -> VLM 识别 -> 日历/导航/TTS 反馈

## 已完成内容

### 1. 方案和项目评估

- 已审阅主目录中的方案文档和 `VisualSemanticAgent` 工程结构。
- 已确认项目当前更接近“高质量原型骨架”，不是完整成品。

### 2. Android 原型主链路补充

- 已补上相机预览与拍照主流程。
- 已补上图片转 Base64 编码逻辑。
- 已补上语音单次识别逻辑。
- 已补上 VLM 网络请求与结构化 JSON 解析逻辑。
- 已补上 Intent 分发逻辑。
- 已补上 TTS 反馈逻辑。
- 已补上加载态与状态文案。
- 已将默认模型接入切换为比赛可用的 vivo 云端接口。
- 已补上快捷场景预设、结果回显卡片、错误弹层和模型响应规范化逻辑。
- 已补上网络有限重试逻辑，用于减轻比赛接口短时波动影响。
- 已补上更细的权限处理逻辑，允许“仅拍照/文字输入”在无麦克风权限时继续使用。
- 已补上更可读的错误分类与重试动作，便于真机测试定位问题。
- 已补上时间字段的更宽松解析逻辑，提高活动入日历场景兼容性。
- 已补上模型 `action` 缺失时的兜底处理，降低接口偶发缺字段导致崩溃的风险。
- 已补上更稳妥的 `ImageProxy -> Bitmap` 转换逻辑，降低部分机型偏色、错位和识别效果下降风险。
- 已补上 TTS 语言可用性兜底，减少部分设备中文播报不可用时的异常风险。
- 已补上语音识别中的按钮重入保护，降低连续点击导致识别器忙状态异常的风险。
- 已对旧架构文档做轻量纠偏，降低历史设计接口和“100% 完成”表述误导当前检查的风险。
- 已补充更常见的活动时间格式兼容与额外单元测试，用于提高后续检查与真机验证稳定性。
- 已完成一轮更细的静态检查，补上若干“真机验证前就值得先修”的稳定性问题。
- 已修正 Gradle 仓库声明冲突与 Compose `ui-graphics` 版本缺失问题，降低 Android Studio Sync / 依赖解析失败风险。
- 已移除当前未实际使用的 Moshi / `kapt` 依赖与对应混淆保留项，减少构建噪音与误导性依赖。
- 已补上协程取消时不误报业务错误的处理，降低切后台、页面销毁或中断时出现错误弹层的风险。
- 已补上 vivo 模型接口的细分错误映射，能够区分限流、权限不足、日额度耗尽等更常见问题。
- 已补上模型请求参数的按模型兼容处理，对 `DeepSeek/Doubao` 与 `qwen` 使用更稳妥的思考开关字段。
- 已补上导航地点的 URL 编码处理，降低中文地点、空格地点在地图跳转时失败的风险。
- 已补上语音初始化与权限状态更一致的处理，减少无麦克风权限时的多余初始化。

### 3. 工程清理

- 已删除手写 `BuildConfig.kt`，改为使用 Gradle 自动生成。
- 已补充缺失的 `res/xml` 配置文件。
- 已修正 manifest 中的基础配置。
- 已将测试文件收敛为最小可用版本，避免大量失配占位测试阻塞编译。

### 4. 仓库文档

- 已更新 `VisualSemanticAgent/README.md`，同步 vivo 比赛接口接入方案与当前真实工程状态。
- 已补充仓库根目录 `README.md`，便于 GitHub 首页直接展示项目概览。
- 已新增 `测试清单.md`，用于后续真机验证与问题记录。
- 已新增 `详细检查清单.md`，用于按步骤执行更细的代码与真机检查。
- 已对 `PROJECT_SUMMARY.md`、`API_GUIDE.md`、`ARCHITECTURE.md`、`QUICKSTART.md` 做轻量纠偏，避免旧设计接口和“100% 完成”表述误导当前检查与测试。
- 已同步 README 与进度文档，明确本轮稳定性修补、当前真实状态与后续验证重点。

## 当前关键文件

- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/MainActivity.kt`
- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/camera/CameraManager.kt`
- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/network/VLMNetworkClient.kt`
- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/intent/IntentDispatcher.kt`
- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/voice/VoiceRecognitionManager.kt`
- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/tts/TextToSpeechManager.kt`
- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/ui/CameraScreen.kt`
- `VisualSemanticAgent/app/src/main/java/com/vsa/visualsemanticagent/ui/LoadingOverlay.kt`

## 当前待办

### 高优先级

- 在 Android Studio 中实际编译并运行一次工程。
- 在 Android Studio 中先完成 Gradle Sync 与一次 Debug 构建。
- 配置比赛提供的 `VLM_API_KEY` 后验证真机闭环。
- 优先验证“海报/活动信息 -> 日历”场景。
- 验证“地点识别 -> 地图导航”场景。

### 中优先级

- 把项目文档中的“100% 完成”表述改成与真实实现一致。
- 把部分旧方案文档中的示例数据模型说明同步到当前 Android 实现状态。
- 增加更真实的单元测试和集成测试。
- 根据 vivo 接口实际返回，确认是否需要继续补充更稳妥的限流重试逻辑。
- 视真机效果决定是否继续扩充寻物与复杂面板导视场景。
- 视真机结果决定是否继续补充相机绑定失败、地图跳转失败等更细粒度日志与埋点。

### 低优先级

- 继续扩展 `tts_feedback` 场景。
- 优化 UI 呈现和答辩演示文案。

## 当前阻塞

- GitHub SSH 认证现已配置完成，可继续正常推送。
- 当前仍需在 Android Studio 中完成真实编译与真机验证。
- 当前尚未在真机上验证 vivo 模型接口的真实返回格式与延迟表现。
- 当前命令行环境未提供 `gradlew`/`gradle`，因此还没有完成命令行构建校验。
- 当前这轮改动已提前修掉若干静态构建风险，但尚未在 Android Studio 中完成实机 Sync / Build 复核。

## 下次开始前建议

1. 先读取本文件。
2. 进入 Android Studio 先做 Gradle Sync 与 Debug 构建。
3. 再进行真实运行验证。
4. 优先验证日历与导航两个主演示场景。
