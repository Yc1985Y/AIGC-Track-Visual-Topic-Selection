# 进度说明

更新日期：`2026-05-10`

## 当前阶段

- 当前阶段：`真实大模型接入完成后的完整工程收口`
- 当前主工程：`VisualSemanticAgent/`
- 当前作品名称：`织时：将校园信息碎片整合为专属时间线的智能助手`
- 当前桌面名称：`织时`

## 本轮核心完成内容

### 1. 真实 vivo 大模型接入完成

当前 Android 工程已接入：

- `https://api-ai.vivo.com.cn/v1/chat/completions`
- `https://api-ai.vivo.com.cn/ocr/general_recognition`
- 模型：`Volc-DeepSeek-V3.2`

本轮不是停留在“卡片展示”层，而是把真实 OCR + LLM 调用接入到了现有业务链路里。

### 2. 旧演示模式主链路已清理

已完成：

- 登录页删除“演示模式”入口
- 主链路不再依赖 mock 回退
- 文本分享、图片分享、相册导入、拍照入口均切到真实模式
- 确认结果中不再展示 Mock 模式标签

### 3. 现有 UI 设计保持不变

本轮工作明确遵守了“不改页面设计，只接真实模型”的要求：

- 未重构当前首页 / 时间线 / 我的页视觉布局
- 仅调整真实调用逻辑与必要文案
- 保持当前答辩展示版 UI 架构不被破坏

## 当前已验证状态

以下内容已在本地真实执行通过：

- `:app:assembleDebug` 构建成功
- APK 重新生成成功
- APK 安装到模拟器成功
- App 启动成功
- 文本分享链路真实调用 vivo 模型成功
- 图片分享链路真实进入 OCR + LLM 成功

### 文本分享已验证结果

测试输入：

- `明天下午三点在图书馆报告厅举行人工智能讲座，请提前十分钟到场签到。`

真实日志结果已出现：

- `action=create_event`
- `title=人工智能讲座`
- `time=明天下午三点`
- `location=图书馆报告厅`

### 图片分享已验证结果

真实媒体 URI 分享测试后，日志已出现：

- `sourceType=share_image`
- `imageBase64Length=43892`
- `action=create_event`
- `title=志愿者通识培训（线上）`
- `location=腾讯会议`

这说明图片并不是本地假卡片，而是真实走通了图片读取、编码、OCR 与大模型解析链路。

## 当前工程结论

截至当前，这份仓库里的《织时》已经具备：

- 完整 Android 工程结构
- 稳定三栏 UI 壳层
- 校园碎片导入能力
- 真实 AI 结构化理解
- 风险判断与确认机制
- 时间线沉淀与提醒基础

因此当前远程仓库应被理解为：

> 一份已接入真实 vivo 大模型服务、可继续装机联调和答辩展示的《织时》完整工程。

## 继续接手建议

后续如果继续推进，建议按这个顺序：

1. 优先在 vivo 实机上完成拍照海报与系统日历联调
2. 再补相对时间到标准时间的本地归一化
3. 最后再继续做更高保真的交互动效与答辩展示优化
